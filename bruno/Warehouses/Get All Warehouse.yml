info:
  name: Get All Warehouse
  type: http
  seq: 1

http:
  method: GET
  url: '{{BASE_URL}}/{{WAREHOUSE_ENDPOINT}}/'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
