info:
  name: Forgot Password
  type: http
  seq: 5

http:
  method: POST
  url: '{{BASE_URL}}/{{AUTH_ENDPOINT}}/forgot-password'
  body:
    type: json
    data: |-
      {
        "email": "nguyen_van_a@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
