info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: '{{BASE_URL}}/{{AUTH_ENDPOINT}}/register'
  body:
    type: json
    data: |-
      {
        "name": "Nguyễn Văn A",
        "email": "nguyen_van_a@gmail.com",
        "username": "12345678"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
